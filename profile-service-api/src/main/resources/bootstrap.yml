---
spring.config:
    activate:
        on-profile: dev, cuber

spring:
    application:
        name: profile-service-api
    cloud.config:
#        uri: http://${SPRING_PROFILES_ACTIVE}-tim-likes-settings-api:8080
      uri: http://localhost:8888
      enabled: true
      failFast: true

---
spring.config:
  activate.on-profile: local

spring.cloud.config:
  enabled: false